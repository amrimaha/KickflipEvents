# nixpacks build config for the Kickflip Python crawler service
# Let nixpacks handle pip install automatically (detects requirements.txt).
# Only add the Playwright browser download as a separate build step.

[phases.build]
cmds = ["playwright install chromium --with-deps"]
